appId: com.noutify.desktop
productName: noutify

directories:
  output: dist
  buildResources: assets

asar: false

files:
  - build/**
  - public/**
  - .next/**/*
  - .next/standalone/**/*
  - .next/standalone/**/node_modules/**

# --- mac configuration: Build a zip for Intel, Apple Silicon, and Universal ---
# mac:
#   category: public.app-category.developer-tools
#   target:
#     - target: zip
#       arch:
#         - x64
#         - arm64
#         - universal

# --- Windows configuration: Three artifacts (User Installer, System Installer, and .zip) for x64 and Arm64 ---
# win:
  # target:
    # User Installer (default NSIS)
    # - target: nsis
    #   arch:
    #     - x64
    #     - arm64
    # System Installer (using NSIS with perMachine set to true)
    # - target:
    #     name: nsis
    #     artifactName: "${productName}-System-Installer-${arch}.${ext}"
    #   arch:
    #     - x64
    #     - arm64
    #   config:
    #     perMachine: true
    # # Archive (.zip)
    # - target: zip
    #   arch:
    #     - x64
    #     - arm64

# --- Linux configuration: Four artifacts (Deb, RPM, Tar.gz, and Snap) for x64, Armv7l, and Arm64 ---
# Note: The Snap target is not available for Armv7l, so it is excluded from that architecture.
linux:
  target:
    # - target: deb
    #   arch:
    #     - x64
    #     - armv7l
    #     - arm64
    # - target: rpm
    #   arch:
    #     - x64
    #     - armv7l
    #     - arm64
    - target: tar.gz
      arch:
        - x64
        # - armv7l
        # - arm64
    # - target: snap
    #   arch:
    #     - x64
    #     - armv7l
    #     - arm64
